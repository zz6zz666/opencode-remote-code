# Test configuration for Remote Code e2e tests
# Copy this file to .env.test and fill in your own values.
# .env.test is gitignored and should never be committed.
REMOTE_SSH=ssh user@your-host
REMOTE_WORKDIR=/tmp/opencode-remote-test
REMOTE_PASSWORD=your-password
REMOTE_SUDO_PASSWORD=your-sudo-password
